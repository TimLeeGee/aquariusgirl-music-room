# 已知問題與踩坑紀錄

#bug #decision

| 現象 | 原本想要 | 過去誤解 | 正確方向 | 驗收 | 來源 | 狀態 |
| --- | --- | --- | --- | --- | --- | --- |
| 只限制匯入併發但逐首仍全庫 map，退化 O(N²) | 大曲庫維持有界工作 | 以為限制 worker 數就足夠 | playlist batch O(P+N)，metadata 只做有界 batch | playlist-batch；10k deterministic 100 commits | 0.1.51 helpers/checks | 已修，真實 10k profiler 待補 |
| 只以 100ms 節流 metadata | commit 數有可驗證上限 | 時間節流等於批數上限 | 使用實際 batch 上限，最多約 100 UI/DB commits | import-work-queue | 0.1.51 checks | 已修 |
| clear discard 後 App busy 沒回 idle | clear 後可立即再匯入 | 忽略 late event | collaboration cancel + generation/discard，clear→idle | manual-import-state | 0.1.51 checks | 已修，GUI 待補 |
| 暫停後視覺化已 idle 仍持續 RAF／render／GC | settled 後不再做背景畫面更新，播放時可恢復 | 只看音量柱已靜止，忽略 hook 仍永久 setState | 最後 0.08 state 先發布再停止；已 idle tick 不配置陣列；effect 依賴負責重啟 | 0.1.50 idle guard、build／dist PASS；Profiler 與打包 GUI 待補 | 0.1.50 checksum | 已修，實測待補 |
| 切 Mini 或 OBS 後播放停住、再按播放誤報被阻擋 | 切模式連續播放 | 視為瀏覽器 autoplay 問題 | 保持 audioElement 在各模式一致位置；有歌但 src 遺失時自癒恢復位置 | 0.1.49 source 與打包 check 已通過；打包 GUI 待補 | README、0.1.49 checksum | 已修，GUI 待補 |
| Windows 更換封面後 select、搜尋和 AI 輸入失焦 | 關閉確認後所有控制正常 | 以為是 overlay 擋住點擊 | 不使用 window.confirm 或 window.alert；renderer confirm dialog 與 native dialog focus restore | 0.1.44 source guards 已通過；Windows 真機仍可回歸驗證 | AGENTS、0.1.44 checksum | 已修 |
| 大封面後 metadata reload 崩潰 | 大小不同封面均可寫回讀回 | 以為是副檔名或 InvalidFormatError | partial read 任何錯誤 fallback 完整讀取；WASM RuntimeError 不可吞成無封面 | 0.1.43 大封面讀回 hash 測試 | AGENTS、0.1.43 checksum | 已修 |
| Windows 播放中套用原始檔失敗 | 不必手動先停歌也可安全寫回 | macOS 成功即代表跨平台成功 | 對目前目標歌暫卸 audio src，writer 對暫時鎖加有限 retry | 0.1.42 checks；Windows 真機仍待補 | AGENTS、0.1.42 checksum | 已修，真機待補 |
| 第二次封面選圖 dirty 消失或保存假成功 | 選圖後正確保持 dirty 並驗證成功 | 只看 preview 或前端 state | selected cover 獨立保存；readbackHash 必須等於 selected hash 且異於舊 hash，再 await 單曲保存 | 0.1.39–0.1.41 roundtrip | AGENTS、0.1.39–0.1.41 checksum | 已修 |
| 清單自訂排序不保存或拖曳太慢 | 排序保存且不拖慢大曲庫 | 全量重寫排序最直接 | 全部歌曲只寫被移動 track 的 ordering key | playlist logic check | AGENTS、0.1.39 checksum | 已修 |
| electron:dev 無法播 file:// 曲目 | 正確區分開發環境與產品行為 | 誤判播放器壞了 | http origin 僅用 blob 拖曳驗證；以打包版驗 file:// | 0.1.49 release note | 0.1.49 checksum | 已知環境限制 |
| 歷史 UI 文件提及內部 bounded scroll，後續版本又調整主視窗捲動 | 依目前 UI 結構調整 | 從舊文件直接套 CSS 結論 | 先讀現在的 AppLayout、PlaylistPanel、TrackList 與最新 AGENTS.md | track-list check | UI review 0.1.16、AGENTS | 可能過時，先回查 |
| Cowork 沙盒 git 寫入後留下刪不掉的 0-byte index.lock，卡死下一個 git 操作 | 沙盒可直接 stage/commit | 以為 git 壞掉，只能靠 Mac 端 .command 清 lock | 根因是 Cowork 掛載資料夾預設無檔案刪除權限；請使用者核准啟用資料夾刪除權限後 git unlink 正常，可直接 commit | 2026-07-10 實測：啟用後 git rm + commit 無殘留 lock | 本頁 2026-07-10 session | 已解 |
| Cowork 沙盒跑 npm run build 失敗（rollup MODULE_NOT_FOUND） | 沙盒完成全部驗收 | 以為程式或設定壞掉 | node_modules 是 mac ARM 版、缺 rollup Linux native module，npm 政策擋補裝；沙盒驗收以 tsc --noEmit + electron:compile + check:* 為準，完整 build 以 Mac 本機 dist:release 為實證 | 2026-07-10 實測；Mac 端打包內含 build | 本頁 2026-07-10 session | 已知環境限制 |

## 回歸前檢

- [ ] 任何 audio 結構改動前，確認 audioElement 的節點位置不變。
- [ ] 任何歌曲資訊改動前，追讀 [[03-功能規格與使用者期待]] 與寫回相關 ADR。
- [ ] 任何 UI dialog 改動前，搜尋 window.confirm 與 window.alert。
- [ ] Windows 結論不能只用 macOS DMG 結果代替。

相關頁面：[[03-功能規格與使用者期待]]、[[05-禁止事項]]、[[06-驗收標準]]、[[09-歷史決策紀錄]]

返回 [[00-入口索引]]。

## 來源

- `AGENTS.md`
- `README.md`
- `docs/releases/0.1.39-checksums.md`
- `docs/releases/0.1.43-checksums.md`
- `docs/releases/0.1.44-checksums.md`
- `docs/releases/0.1.49-checksums.md`
- `docs/releases/0.1.50-checksums.md`
- `docs/releases/0.1.51-checksums.md`
