# Aquariusgirl Music Room 0.1.52 Checksums

日期：2026-07-15
驗收更新：2026-07-16
交付範圍：source、文件與本 checksum 同步 GitHub `main`；installer 位於 `release-delivery/installers/`，不進 Git、不上傳。

| 檔案 | Bytes | SHA-256 |
| --- | ---: | --- |
| `Aquariusgirl Music Room-0.1.52-arm64.dmg` | 684,805,233 | `97c92ee773a3eb27dfaeb5b49f518c9436ee8c9b2e052d9ecc048d953ca5fb4d` |
| `Aquariusgirl Music Room Setup 0.1.52.exe` | 667,678,858 | `3e4a6d5d3ee7a1b4e6e25bb770518f5e34be9d8ba525230d74c1cc01f1aa3b54` |

## 已驗證

- `npm run dist:release` exit 0，包含 `check:theme-colors`、`check:cover-colors`、既有 canonical checks、AI assets、build 與 Electron compile。
- 主環境另跑 `check:cover-colors`、`check:theme-colors`、`check:playback-restore`、`check:metadata-save-loop`、`check:audio-visualizer-idle`、Mini opacity check、`npm run build`、`npm run electron:compile`、`git diff --check`，均 PASS。
- DMG：`hdiutil verify` VALID，overall CRC32 `$3F6DEC31`；唯讀掛載讀回 CFBundleShortVersionString／CFBundleVersion 均為 0.1.52；app main 為 Mach-O arm64；TagLib wasm、AI model 與 darwin-arm64 llama-server 存在。
- EXE：PE32 GUI Intel 80386；Nullsoft NSIS self-extracting archive static PASS。
- packaged macOS 隔離 profile：Header「外觀設定」、Switch 預設 off／重啟保存、無封面播放回手動 195/232、紅色封面 harness effective primary/mini=0、播放結束仍保留、關閉後漸變回 195/232、底部與桌面 Mini 同步、Mini opacity 92。
- 使用者回報 EXE／DMG 的新增功能簡單 smoke test 正常；未提供 OS 版本與逐項手順。
- 既有同步流程已放入 `release-delivery/installers/`，release 暫存已清。

## 未驗證與交付邊界

- 未簽章、未 notarize。
- 未做 Windows 完整回歸；使用者 EXE smoke test 不等於逐項 Windows QA。
- 未以真實 MP3／FLAC／M4A 內嵌封面驗證取色；有封面 GUI 證據來自隔離 IndexedDB harness。
- 未在 packaged UI 快速連續切換三首真實歌曲；A→B→C stale-result 由 deterministic check 驗證。
- 未用真實 10k 音樂檔量測 CPU、RSS 或 Profiler；架構只分析目前歌曲、快取上限 64。
- source、文件與 checksum 已 commit／push 到 GitHub `main`；未建立 tag、PR、GitHub Release，未上傳 installer。
