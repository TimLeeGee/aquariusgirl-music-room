# Aquariusgirl Music Room 0.1.38 Checksums

Date: 2026-07-05

## Installers

| File | Size | SHA-256 |
| --- | ---: | --- |
| `Aquariusgirl Music Room Setup 0.1.38.exe` | 667,664,556 bytes | `fd07376a35cbeccdec55d98751a2273fe67834904c16f24d9d112f71825d5da8` |
| `Aquariusgirl Music Room-0.1.38-arm64.dmg` | 684,757,578 bytes | `66a547f31c535ad9643ba2d7c2ea7bebedc62130b114ba543ca50aa8fccf7a7a` |

## Verification

- `npm run check:song-info` passed, including JPEG / PNG MIME alias checks and the real Plazma temp-copy cover roundtrip when the fixture env vars were set.
- `npm run check:track-list-virtualization` passed, including the seven playlist sort options and sort select visibility guard.
- `npm run check:playback-order`, `npm run check:playback-restore`, `npm run check:metadata-save-loop`, `npm run build`, and `npm run electron:compile` passed.
- `npm run dist:release` passed and synced both installers to `release-delivery/installers/`; temporary `release/` output was removed.
- DMG `hdiutil verify` was VALID.
- Read-only DMG mount confirmed `CFBundleShortVersionString` and `CFBundleVersion` are `0.1.38`, packaged `app.asar` package version is `0.1.38`, the executable is Mach-O `arm64`, and `Contents/Resources/taglib-wasm/taglib-web.wasm` exists outside `app.asar`.
- Packaged `app.asar` readback confirmed the renderer contains the playlist sort label and the renderer/main process contain the 0.1.38 cover MIME aliases.
- Packaged macOS GUI was opened from the DMG with an isolated profile and temporary Plazma copies; the playlist sort select showed all seven options. The native cover file picker could be opened by real mouse click, but full mouse-only cover selection was blocked by a macOS privacy prompt that requires explicit user approval. Cover writeback itself was verified with the isolated temp-copy writer/readback harness.
- Windows artifact was identified as a PE32 Nullsoft NSIS installer; real Windows runtime QA was not performed on this macOS machine.

## Recheck Commands

```bash
shasum -a 256 "release-delivery/installers/Aquariusgirl Music Room Setup 0.1.38.exe" "release-delivery/installers/Aquariusgirl Music Room-0.1.38-arm64.dmg"
hdiutil verify "release-delivery/installers/Aquariusgirl Music Room-0.1.38-arm64.dmg"
file "release-delivery/installers/Aquariusgirl Music Room Setup 0.1.38.exe"
```
