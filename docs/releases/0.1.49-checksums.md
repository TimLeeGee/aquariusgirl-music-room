# 0.1.49 Installer Checksums

日期：2026-07-10

| 檔案 | bytes | SHA-256 |
|------|-------|---------|
| `Aquariusgirl Music Room Setup 0.1.49.exe` | 667,675,017 | `7c3708ddba7abb9e81aa934575bf95af7e290b2293e77b8f89589741993cabf6` |
| `Aquariusgirl Music Room-0.1.49-arm64.dmg` | 684,771,178 | `ee8ef2aeaa88a474fd5dad9986051223c4abfae66a3c6d90c7cb4cdf49f3e27a` |

- 內容：Mini/OBS 切換播放中斷 hotfix（0.1.48 回歸：`<audio>` 被 `TextOverrideContext.Provider` 多包一層、切模式時 React 依位置重建節點；移回 `BrandAssetsContext.Provider` 首子節點）＋播放自癒保險（`useAudioPlayer`：偵測「有歌但節點沒音源」自動重掛＋恢復位置；`togglePlay` 壞狀態第一下改重新播放；寫檔暫停期間自癒停用；`describePlayError` 錯誤分流——只有 `NotAllowedError` 才報「阻擋」）＋ AI 聊天視窗 UX（快捷泡泡列 sticky 置頂、首次互動後收合 hover 下拉、訊息底部錨定由下往上長、聊天區 256px→500px；圓角間距不變）。
- DMG `hdiutil verify`：VALID（CRC32 $21504B8A）
- DMG 唯讀掛載讀回：`CFBundleShortVersionString` / `CFBundleVersion` 均為 0.1.49、Mach-O arm64、taglib wasm 存在（見 `qa-temp/dist-0.1.49-result.txt`）
- EXE static check：PE32 executable (GUI)、Nullsoft NSIS self-extracting installer
- 打包指令：`npm run dist:release`（`qa-temp/build-0.1.49.command` 於 Mac 本機執行），DIST_EXIT=0；沙盒 `tsc --noEmit`、`electron:compile`、全部可跑 check 均通過
- dev 模式已知限制（非 bug）：`electron:dev` 渲染來自 http origin，Chromium 禁止載入 `file://` 音源；dev 驗證播放請用拖曳（blob），打包版不受影響。
- 未簽章 / 未 notarize；打包版 GUI 實測（切 Mini 續播、泡泡收合、訊息貼底、清單捲動）與 Windows 真機待補；本次已推送 GitHub `main`
